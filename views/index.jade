extends layout

block content
  h1= title
  p Welcome to #{title}

  #messages
    ul
      - for (var i = messages.length - 1; i >= 0; i--)
        li
          p= messages[i].text
          p= messages[i].user
          p= messages[i].added

